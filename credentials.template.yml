# Copy this file to conf/local/credentials.yml and fill in your Adzuna keys.
# Sign up at https://developer.adzuna.com/ to get them.
#
# conf/local/credentials.yml is gitignored — never commit real credentials.

adzuna:
  app_id: YOUR_ADZUNA_APP_ID
  app_key: YOUR_ADZUNA_APP_KEY
